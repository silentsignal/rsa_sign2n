[project]
name = "rsa-sign2n"
version = "0.1.0"
description = "RSA signature forgery tool ported to uv"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "asn1tools>=0.166",
    "gmpy2>=2.2",
    "pycryptodome>=3.20",
]

[tool.uv]
managed = true